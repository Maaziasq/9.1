<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="roundedview" parent="">
        
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50dp</item>
        <item name="color">@color/white</item>

    </style>

    <style name="roundedicon" parent="">

        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20dp</item>
        <item name="borderWidth">8px</item>
        <item name="color">@color/white</item>
    </style>

    <style name="Spinner" parent="Widget.AppCompat.Light.DropDownItem.Spinner">
        <item name="android:paddingStart">0dp</item>
        <item name="android:paddingEnd">0dp</item>
        <item name="android:textColor">@color/accentpurple</item>
        <item name="android:textSize">12dp</item>
        <item name="android:dropDownSelector">@color/accentpurple</item>
    </style>

    <style name="SCBSwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/accentpurple</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/accentpurple</item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/accentpurple
        </item>
    </style>

</resources>